from django.apps import AppConfig


class productDataConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'productData'

    # def ready(self):
    #     import productData.signals
                        
